package com.example.application9sqlandjdbc;

public class PostgresCredentials extends DBConnectionCredentials {
    public PostgresCredentials(String connection, String username, String password) {
        super(connection, username, password);
    }
}
